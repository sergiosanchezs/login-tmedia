import styled from 'styled-components';

export const LinkContainer = styled.div`
  justify-self: center;
  align-self: center;
  margin: 0.5rem 0;

  span {
    margin-right: 0.5rem;
    font-family: 'Roboto-Light';
    font-weight: 500;
  }

  a {
    color: black;
  }
`;
